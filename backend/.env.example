# MongoDB Configuration
# 
# OPTION 1: MongoDB Atlas (Cloud Database - RECOMMENDED for cross-device use)
# Get your connection string from: https://cloud.mongodb.com/
# Format: mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/medora-reach?retryWrites=true&w=majority
# Replace <password> with your actual password and add database name before the ?
MONGO_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/medora-reach?retryWrites=true&w=majority
#
# OPTION 2: Local MongoDB (Only works on this device)
# MONGO_URI=mongodb://localhost:27017/medora-reach

# Server Configuration
PORT=5001

# JWT Secret (IMPORTANT: Change this to a secure random string in production)
# You can generate one at: https://randomkeygen.com/
JWT_SECRET=your-secret-key-change-this-in-production
